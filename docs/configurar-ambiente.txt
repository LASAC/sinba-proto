git clone git@github.com:LASAC/SINBA.git
cd SINBA/project/sinba
cp .env.example .env
composer install
php artisan key:generate

# configurar .env com informações de bd...
# criar o bd

php artisan migrate

# rodar o servidor (artisan, padrão porta 8000):
php artisan serve